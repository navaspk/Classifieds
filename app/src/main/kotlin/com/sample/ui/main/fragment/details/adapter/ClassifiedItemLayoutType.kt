package com.sample.ui.main.fragment.details.adapter

/**
 * To get different type of items, so that we can differentiate easily with different
 * layout, in future we can add multiple types items
 */
enum class ClassifiedItemLayoutType {
    TYPE_CLASSIFIED_ITEM_HEADING,
    TYPE_CLASSIFIED_ITEM_DESC,
    TYPE_CLASSIFIED_ITEM_BRAND
}
